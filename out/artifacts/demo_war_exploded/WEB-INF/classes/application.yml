server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/xzshop?useSSL=false&serverTimeZone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

  main:
    allow-circular-references: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

#映射文件
mybatis:
  mapper-locations: classpath:mapper/*.xml